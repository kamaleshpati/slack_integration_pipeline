#sudo: required
#services:
#  - docker
#before_install:
#  - docker build -t kamaleshpati/slack_integration_pipeline  -f Dockerfile.dev .
language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - python manage.py migrate
script:
#  - docker run -e CI=true kamaleshpati/slack_integration_pipeline
  - python manage.py test